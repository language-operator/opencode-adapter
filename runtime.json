{
  "schemaVersion": 1,
  "name": "opencode",
  "displayName": "opencode",
  "role": "main",
  "requires": { "codingRuntime": ">=0.1.0 <1.0.0" },
  "env": { "OPENCODE_CONFIG_DIR": "${STATE_DIR}/opencode" },
  "preflight": [{ "path": "${STATE_DIR}/opencode", "mode": "700" }],
  "config": { "emitter": { "type": "module", "path": "/opt/adapter/emit.mjs" } },
  "serve": { "surface": "terminal", "port": 8080, "originGuard": true },
  "terminal": {
    "tmuxSession": "opencode",
    "launch": ["launch-opencode"],
    "cwd": "${WORKDIR}",
    "wsPath": "/ws",
    "keepaliveSeconds": 25
  }
}
