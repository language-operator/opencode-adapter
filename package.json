{
  "name": "opencode-adapter",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Combined opencode runtime: operator config adapter (init) + xterm.js / tmux WebSocket terminal server (main).",
  "dependencies": {
    "@xterm/addon-clipboard": "^0.2.0",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "node-pty": "^1.0.0",
    "ws": "^8.18.0",
    "yaml": "^2.9.0"
  }
}
